/**
 * Write a program that returns the closest temperature to 0 among an array of temperatures.
 * If two numbers are equally close to zero, positive integer has to be considered closest to zero
 * (For instance, if the temperatures are -5 and 5, then return 5).
 * If no temperatures are provided, return 0.
 */
export default function closestTemperature(temperatures: number[]): number {
  throw new Error("Not implemented");
}
